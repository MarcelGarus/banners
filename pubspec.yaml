name: banners
description: Package for dynamically displaying global banners at the top of the screen.
homepage: https://github.com/marcelgarus/banners
version: 0.1.0

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ">=1.0.0 <2.0.0"
