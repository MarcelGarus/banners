name: banners
version: 0.3.0
description: Package for dynamically displaying global banners at the top of the screen.
repository: https://github.com/marcelgarus/banners

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  collection: ^1.15.0
  flutter:
    sdk: flutter
